# Scope A — TenantMiddleware: fast unverified JWT decode to extract tid,
#            resolves tenant from TenantRegistry, attaches to request.state
# Owner: Graph + Routes team
# Reference: CONTEXT.md Section 12 (middleware behaviour, PUBLIC_PATHS)
