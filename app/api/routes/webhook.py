# Scope A (route shell only) — POST /webhook/register, POST /webhook/call-records
# Owner: Graph + Routes team (route handlers)
# NOTE: webhook.py in services/graph/ is NOT Scope A — owned by webhook team
# This file only defines route handlers that call into services/graph/webhook.py
